def calculate_total(items):
    total = 0
    for item in items
        total += item["price"]
    return total

def apply_discount(total, discount):
    if discount > 0
        return total - (total * discount
    return total
